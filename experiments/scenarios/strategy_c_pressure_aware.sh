#!/usr/bin/env bash

set -euo pipefail

export SCENARIO="${SCENARIO:-strategy_c_pressure_aware}"
export TIERING_POLICY_VARIANT="${TIERING_POLICY_VARIANT:-C}"
export TIERING_TRIGGER_MODE="${TIERING_TRIGGER_MODE:-threshold}"
export TIERING_THRESHOLD_CHECK_SEC="${TIERING_THRESHOLD_CHECK_SEC:-5}"
export TIERING_THRESHOLD_COOLDOWN_SEC="${TIERING_THRESHOLD_COOLDOWN_SEC:-5}"
export TIERING_IDLE_STABLE_ROUNDS="${TIERING_IDLE_STABLE_ROUNDS:-3}"
export TIERING_IDLE_CPU_PCT="${TIERING_IDLE_CPU_PCT:-70}"
export TIERING_IDLE_MEMORY_PCT="${TIERING_IDLE_MEMORY_PCT:-90}"
export TIERING_IDLE_IOWAIT_PCT="${TIERING_IDLE_IOWAIT_PCT:-20}"
export TIERING_IDLE_QUEUE_DEPTH="${TIERING_IDLE_QUEUE_DEPTH:-16}"
export AGE_THRESHOLD_SEC="${AGE_THRESHOLD_SEC:-0}"
export MAX_OBJECTS_PER_ROUND="${MAX_OBJECTS_PER_ROUND:-25}"
export MAX_BYTES_PER_ROUND="${MAX_BYTES_PER_ROUND:-33554432}"
export WORKER_BW_LIMIT_MBPS="${WORKER_BW_LIMIT_MBPS:-8}"
export PRESSURE_PROFILE="${PRESSURE_PROFILE:-cpu}"
export PRESSURE_DURATION_SEC="${PRESSURE_DURATION_SEC:-60}"
export PRESSURE_WARMUP_SEC="${PRESSURE_WARMUP_SEC:-10}"

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec "${SCRIPT_DIR}/_run_local_scenario.sh" "$@"
